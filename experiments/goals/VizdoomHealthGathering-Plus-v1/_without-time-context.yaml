# Clear the inherited finite-horizon model-input contract before the Plus goal
# installs its health-only context in the following composition layer.
train:
  environment:
    task:
      model_inputs: null
eval:
  environment:
    task:
      model_inputs: null

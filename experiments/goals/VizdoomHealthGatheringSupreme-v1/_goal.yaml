defaults:
- ../_vizdoom-common@_global_
- _self_
title: ViZDoom Health Gathering Supreme survival
train:
  environment:
    env_config:
      env_args: &health_gathering_supreme_env_args
        info_filter:
          mode: all
          keys: [player_dead]
    task: &health_gathering_supreme_task
      signals:
        player_dead: player_dead
      events:
        player_died: {signal: player_dead, operation: equals_for, value: 1, steps: 1}
        survived: {signal: player_dead, operation: equals_for, value: 0, steps: 512}
      termination:
        failure: [player_died]
        success: [survived]
      reward:
        reward_scale: 100.0
eval:
  environment:
    env_config:
      env_args: *health_gathering_supreme_env_args
    task: *health_gathering_supreme_task

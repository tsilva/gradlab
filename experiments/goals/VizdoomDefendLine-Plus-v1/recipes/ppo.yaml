defaults:
- ../../../recipes/_presets/vizdoom-defend-line-ppo@_global_
- _self_
recipe_id: ppo
description: >-
  Seed {{ seed }} trains PPO from pixels to defend the line and maximize native combat return,
  until the training target, timestep cap, or return plateau. This run is training-only; goal-owned
  evaluation remains available for later explicit use.

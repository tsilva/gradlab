# Compose this variant from reusable YAML configuration fragments.
defaults:
# Load the complete Basic goal under this variant's global identity.
- ../VizdoomBasic-v1/_goal@_global_
# Apply this file last so variant-specific values can override the inherited contract.
- _self_
# Give the variant its operator-facing title while retaining the inherited task semantics.
title: ViZDoom Basic first-shot accuracy

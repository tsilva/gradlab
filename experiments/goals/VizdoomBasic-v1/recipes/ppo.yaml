defaults:
- ../../../recipes/_presets/atari-ppo@_global_
- _self_
recipe_id: ppo
description: Training-only pixel PPO for {{ goal_id }} with scaled rewards.
train:
  checkpoint_eval_backend: none
  timesteps: 2000000
  early_stop:
    conditions:
      return_plateau:
        start_after_steps: 500000
        patience_steps: 500000

# Historical control goal retained under its stable identity; it now follows
# the shared Mario level-attempt termination contract.
defaults:
- ../_goal-common@_global_
- ../_reward-shapes-common@_global_
- _self_

# This control goal uses the same provider start state as Level1-1.
template_vars:
  state: Level1-1

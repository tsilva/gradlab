interface:
  display_name: "Build Release"
  short_description: "Build and publish validated PyPI releases"
  default_prompt: "Use $build-release to cut, publish, monitor, and verify the next GradLab PyPI release."

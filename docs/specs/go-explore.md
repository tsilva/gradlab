# Go-Explore

This specification applies only to Go-Explore Runs and Checkpoints.

## Requirements

- Every Go-Explore Checkpoint must store a portable cell-and-route graph.
- A Go-Explore Checkpoint must omit its archive snapshot unless its Run Configuration explicitly requests one.
